




package {{package}};

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;
import java.util.List;
import java.util.Optional;
import com.example.demo.entity.{{className}};
import com.example.demo.service.{{className}}Service;

@RestController
@RequestMapping("/api/{{className}}")
public class {{className}}Controller {
    @Autowired
    private {{className}}Service service;

    @GetMapping
    public List<{{className}}> getAll() {
        return service.findAll();
    }

    @GetMapping("/{id}")
    public Optional<{{className}}> getById(@PathVariable {{#if idClassName}}{{idClassName}}{{else}}Long{{/if}} id) {
        return service.findById(id);
    }

    @PostMapping
    public {{className}} create(@RequestBody {{className}} entity) {
        return service.save(entity);
    }

    @PutMapping("/{id}")
    public {{className}} update(@PathVariable {{#if idClassName}}{{idClassName}}{{else}}Long{{/if}} id, @RequestBody {{className}} entity) {
        return service.update(id, entity);
    }

    @DeleteMapping("/{id}")
    public void delete(@PathVariable {{#if idClassName}}{{idClassName}}{{else}}Long{{/if}} id) {
        service.delete(id);
    }
}
