package {{package}};

import jakarta.persistence.*;
import java.io.Serializable;
{{#if (anyFieldType fields "LocalDate")}}
import java.time.LocalDate;
{{/if}}

@Entity
@Table(name = "{{tableName}}")
{{#if idClassName}}
@IdClass({{idClassName}}.class)
{{/if}}
public class {{className}} implements Serializable {
{{#each fields}}
    {{#if isId}}
    @Id
    {{#if isAuto}}
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    {{/if}}
    {{/if}}
    @Column(name = "{{name}}")
    private {{javaType}} {{name}};
{{/each}}

    // Getters y Setters
{{#each fields}}
    public {{javaType}} get{{capitalize name}}() { return {{name}}; }
    public void set{{capitalize name}}({{javaType}} {{name}}) { this.{{name}} = {{name}}; }
{{/each}}
}

{{!-- Helper para capitalizar --}}
{{!-- Se debe registrar en el código si se usa fuera de handlebars-cli --}}
